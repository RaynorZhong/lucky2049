"""lucky2049 application package (FastAPI server + draw engine).

The standalone auditor `verify.py` deliberately lives at the repo root, not in
this package, so it stays copy-paste runnable as `python verify.py <id>`.
"""
